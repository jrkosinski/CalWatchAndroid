package com.calwatch.android.tests;

import android.graphics.Paint;

import org.junit.Test;

/*
import org.openqa.selenium.By;
import org.openqa.selenium.WebDriver;
import org.openqa.selenium.remote.CapabilityType;
import org.openqa.selenium.remote.DesiredCapabilities;

//import io.selendroid.client.SelendroidDriver;
import io.selendroid.client.SelendroidDriver;
import io.selendroid.common.SelendroidCapabilities;

import io.appium.java_client.AppiumDriver;

import org.openqa.selenium.WebDriver;
import org.openqa.selenium.remote.CapabilityType;
import org.openqa.selenium.remote.DesiredCapabilities;
import org.testng.annotations.Test;
**/

import java.io.File;
import java.util.concurrent.TimeUnit;

/**
 * Created by John R. Kosinski on 28/1/2559.
 */
public class AppiumTest {

    @Test
    public void runTest() throws Exception {

        /*
        SelendroidCapabilities capabilities = new SelendroidCapabilities("");
        //DesiredCapabilities capabilities = new DesiredCapabilities();
        capabilities.setCapability(CapabilityType.BROWSER_NAME, "");
        capabilities.setCapability("deviceName", "ASUS ASUS_Z007");
        capabilities.setCapability("platformVersion", "4.4.4");
        capabilities.setCapability("platformName", "Android");
        //capabilities.setCapability("app", app.getAbsolutePath());

        capabilities.setCapability("appPackage", "com.calwatch.android");
        capabilities.setCapability("appActivity", "com.calwatch.android.activities.MainActivity");
        WebDriver driver = new SelendroidDriver(capabilities.device(""));

        driver.manage().timeouts().implicitlyWait(60, TimeUnit.SECONDS);
        driver.findElement(By.xpath("//Button[@id='loginButton']")).click();
        File app = new File("D:\\Clients\\TopTal\\CodingProject\\project-john-r-kosinski\\CalWatchAndroid\\app\\build\\outputs\\apk\\app-debug.apk");


        //WebDriver driver = new AppiumDriver<String>()
        */
    }
}
